"use strict";
// Regex that finds { and } so they can be removed on a lookup for string format
var FORMAT_ARGS_REGEX = /[\{\}]/g;
// Regex that finds {#} so it can be replaced by the arguments in string format
var FORMAT_REGEX = /\{\d+\}/g;
/**
 * String Format, like C# string format.
 * Usage Example: stringFormat("hello {0}!", "mike") will return "hello mike!"
 */
function stringFormat(s) {
    var values = [];
    for (var _i = 1; _i < arguments.length; _i++) {
        values[_i - 1] = arguments[_i];
    }
    return s.replace(FORMAT_REGEX, function (match) {
        var matchId = match.replace(FORMAT_ARGS_REGEX, '');
        var matchValue = values[matchId];
        if (!matchValue && DEBUG) {
            // If the wrong number of arguments is specified, show an alert in debug mode.
            alert("Argument number " + matchId + " is missing. stringFormat was called with format " + s);
        }
        return matchValue;
    });
}
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = stringFormat;

//# sourceMappingURL=stringFormat.js.map
