import { IDeveloperToolsTab } from './IDeveloperToolsTab';
export default class DeveloperToolsLoader {
    private static _instance;
    static readonly instance: DeveloperToolsLoader;
    private _developerTools;
    private _developerToolsTabsBacklog;
    /**
     * Registers a tab in the developer tools. The developer tools are invoked by pressing "CTRL+SHIFT+~".
     *
     * @param {developerToolsTab} The tab definition.
     *
     * @internal
     */
    registerDeveloperToolsTab(developerToolsTab: IDeveloperToolsTab): void;
    /**
     * Shows/hides the developer tools panel.
     *
     * @internal
     */
    toggleDeveloperTools(): void;
    private _tryFillReactFlux();
    private _tryFillDevToolsFabric();
    private _injectReactFluxIntoCache(react, reactDom, flux);
    private _loadDeveloperTools(loadFabric);
    private _initializeDeveloperTools(developerToolsClass);
}
